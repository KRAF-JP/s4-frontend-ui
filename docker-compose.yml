version: '3'
services:
  ui:
    build:
      context: .docker/ui
      dockerfile: Dockerfile
    #command: yarn dev
    ports:
      - "3001:3001"
      - "6006:6006"
    expose:
      - 3000
    volumes:
      - ./:/var/www
    stdin_open: true
    tty: true
    restart: always


